idf_component_register(
    SRCS
        "{{COMPONENT_NAME}}.c"
    INCLUDE_DIRS
        "."
        "include"
    REQUIRES                    # 关键：ESP-IDF v5.5.4 必须显式声明依赖
        "freertos"
        "esp_driver_gpio"
        "nvs_flash"
)

# 抑制第三方代码警告
target_compile_options(${COMPONENT_LIB} PRIVATE
    -Wno-unused-parameter
    -Wno-unused-variable
)
