idf_component_register(
    SRCS
        "demo_main.c"
    INCLUDE_DIRS
        "."
        "../{{APP_NAME}}/main"    # 引用 bsp.h 等最小系统头文件
    REQUIRES
        "{{COMPONENT_NAME}}"
        "bsp"                      # 硬件最小系统（可选，视实际依赖）
)
