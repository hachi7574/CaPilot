<!DOCTYPE html>
<html lang="zh-CN">
<head>
  <meta charset="utf-8">
  <title>TRAE AI 创造力大赛报名帖 - CaPilot</title>
  <style>
    body {
      font-family: "Microsoft YaHei", "PingFang SC", sans-serif;
      line-height: 1.7;
      margin: 36px 42px;
      color: #111;
      font-size: 12pt;
    }
    h1 {
      font-size: 22pt;
      margin: 0 0 8px;
    }
    h2 {
      font-size: 16pt;
      margin: 24px 0 10px;
    }
    p {
      margin: 8px 0;
    }
    .meta {
      font-weight: 700;
      margin-bottom: 18px;
    }
    .label {
      font-weight: 700;
    }
    ul {
      margin: 6px 0 10px 24px;
    }
    li {
      margin: 4px 0;
    }
  </style>
</head>
<body>
  <h1>TRAE AI 创造力大赛报名帖</h1>
  <p class="meta">赛道：[硬件交互]</p>

  <h2>1. 创意名称 + 创意介绍</h2>
  <p><span class="label">创意名称：</span>CaPilot</p>
  <p><span class="label">想解决什么问题：</span>开发者同时跑多个 AI Agent（编码、调试、文档、测试等）时，一套键盘、鼠标和单显示器很难高效管理多个并发会话。每看一个 Agent 都要切窗口、翻日志、找上下文，一天累计半小时以上都耗在“找窗口”和“回状态”上，而这些本来应该是 AI 帮人省掉的成本。</p>
  <p><span class="label">为什么会想到做这个：</span>我自己同时在做桌面宠物、人形机器人、博客等多个项目，几乎每天都在多个 Agent 之间反复切换。Agent 的输出散落在终端、浏览器和聊天窗口里，没有统一的物理指挥界面。刚好手头的立创实战派 ESP32-S3 开发板已经带了触摸屏、麦克风阵列和 6 轴陀螺仪，所以我想把“触摸板代替鼠标、语音代替键盘、体感代替快捷键”整合进一个设备，真正服务多 Agent 工作流。</p>
  <p><span class="label">大概是什么产品：</span>一块 USB 即插即用的桌面小屏外设，提供 Agent 管理、触控板、语音三种模式一键切换，让开发者双手不离主键盘也能指挥所有 AI Agent。</p>

  <h2>2. 目标用户及痛点</h2>
  <p><span class="label">面向哪些用户：</span>多 Agent 工作流重度用户，包括独立开发者、同时调度多个 Agent 的工程师，以及使用 Trae Work / Code 自定义 Agent 的 AI 工作流用户。它最核心的用户画像是：主键盘上的双手已经很忙，还要分神去管理一堆 Agent。</p>
  <p><span class="label">在什么场景下使用：</span>当用户正在编码，需要快速瞥一眼各个 Agent 在做什么、有没有新结果、是否需要自己介入时，可以直接看设备状态；当双手不想离开键盘时，也可以在设备屏幕上滑动代替找鼠标、对着设备说一句话代替键盘输入、甩一下设备代替 Alt+Tab 切窗口。</p>
  <p><span class="label">当前痛点：</span></p>
  <ul>
    <li>切换成本高：每次在 Agent 间切换都要 5~15 秒，高频打断深度工作状态。</li>
    <li>状态不可见：Agent 的忙闲、产出和是否需要确认，都要靠逐个翻窗口才能知道，关键结果很容易被日志淹没。</li>
    <li>手部移动多：双手要在键盘和鼠标之间来回切换，桌面空间有限，也不适合再摆第二套输入设备。</li>
    <li>效率上限被卡：现有工具链限制了一个人能同时驱动的 Agent 数量，AI 越强，这个管理瓶颈越明显。</li>
  </ul>

  <h2>3. 价值与意义</h2>
  <p><span class="label">效率提升：</span>CaPilot 目标是把 Agent 管理操作时间降低约 80%，把单次切换、查看、下指令的操作压到 2 秒以内，做到“零窗口切换”和“一瞥即知全局”。这样开发者才能真正稳定地同时驱动 10 个以上 Agent，而不是被切换成本拖慢。</p>
  <p><span class="label">品类价值：</span>现有外设像 Stream Deck、TourBox、Magic Trackpad，要么是纯宏按键，要么是纯触控输入，没有一款是围绕“多 Agent 指挥”设计的。CaPilot 把 Agent 状态感知、触摸板、语音、体感集成到同一块小屏上，尝试验证一件事：AI 原生工作模式，值得拥有自己的专属硬件入口。</p>
</body>
</html>
