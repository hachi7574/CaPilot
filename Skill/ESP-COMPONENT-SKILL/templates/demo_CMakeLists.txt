# Top-level CMakeLists.txt for demo_{{COMPONENT_NAME}}
# 独立 Demo 工程，仅依赖 {{COMPONENT_NAME}} 组件 + BSP 最小系统

cmake_minimum_required(VERSION 3.16)

# 引入组件搜索路径：先搜本工程 components/，再向上搜共享组件
set(EXTRA_COMPONENT_DIRS
    "${CMAKE_CURRENT_SOURCE_DIR}/../{{APP_NAME}}/components"
)

include($ENV{IDF_PATH}/tools/cmake/project.cmake)
project(demo_{{COMPONENT_NAME}})
